import SuccessSearch from '@/components/SuccessSearch'
import React, { Suspense } from 'react'

function page() {
  return (
    <Suspense>
        <SuccessSearch />
    </Suspense>
  )
}

export default page